# UI Bugs — Production Sweep 2026-05-18

Baseline: `npm run release:check` on `main` exited 0 before changes.

Manual inventory method: Playwright is not installed in this repo. Used route/API inventory from `app/**/page.tsx` and `app/**/route.ts`, static link/CTA scan, code smoke against `docs/references/design-checklist.md`, and existing release gate. Viewport risk checked from CSS/component structure for 320 / 375 / 768 / 1024 / 1440 px. Staging URL and production credentials were not available in this local session, so Phase 4 staging/container checks remain operator-run.

## Bugs

| BUG-id | Route + viewport | Severity | Expected | Observed | Hypothesised root cause | Linked file |
| --- | --- | --- | --- | --- | --- | --- |
| BUG-001 | `/dashboard`, mobile bottom nav 320–1023 px for Superadmin | 2 | `docs/references/design-checklist.md:114`–`docs/references/design-checklist.md:120` require Beranda, Cabang, Approval, Laporan `/dashboard/reports/attendance`, Akun | Superadmin primary nav exposes Beranda, Approval, Karyawan, Laporan `/dashboard/reports`, Akun; Cabang missing and Laporan opens overview first | `navigationPolicy` primary set predates final reference contract | `lib/navigation/role-navigation.ts`, `tests/rbac/role-navigation.test.ts` |
| BUG-002 | `/dashboard/locations`, all viewports; delete CTA | 2 | `docs/references/design-checklist.md:102`–`docs/references/design-checklist.md:105` forbid raw `confirm()` / `alert()` for destructive actions | Delete location uses browser `confirm()`, inconsistent with card/modal vocabulary | Destructive CTA implemented before production modal contract | `app/dashboard/locations/page.tsx` |
| BUG-003 | `/dashboard/locations`, modal form at 320 / 375 px | 2 | `docs/references/design-checklist.md:96` requires forms with visible labels and ≥44 px tap targets; `docs/references/design-checklist.md:143` requires cards not break 320 px viewport | Latitude/longitude/radius fields are forced into 3 columns; fields can compress below comfortable tap/input width on 320 px | Inline fixed `gridTemplateColumns: "1fr 1fr 1fr"` without mobile-first breakpoint | `app/dashboard/locations/page.tsx` |
| BUG-004 | `/dashboard/reports`, report type selector at 320 / 375 px | 2 | `docs/references/design-checklist.md:96` requires forms/buttons with ≥44 px tap targets and working states | Three report-type buttons forced into 3 columns; labels crowd on iPhone SE | Inline `repeat(3, 1fr)` without mobile-first wrapping | `app/dashboard/reports/page.tsx` |
| BUG-005 | `/dashboard/reports`, PDF export CTA | 2 | `docs/references/design-checklist.md:102`–`docs/references/design-checklist.md:105` forbid raw `confirm()` / `alert()` on production pages | PDF export uses `alert()` for unavailable export | Placeholder feedback left in production page | `app/dashboard/reports/page.tsx` |
| BUG-006 | `/dashboard/notifications`, delete CTA | 2 | `docs/references/design-checklist.md:102`–`docs/references/design-checklist.md:105` forbid raw `confirm()` / `alert()` for destructive actions | Delete notification uses browser `confirm()` | Destructive CTA implemented before modal contract | `app/dashboard/notifications/page.tsx` |
| BUG-007 | `/dashboard/leave`, request type selector at 320 / 375 px | 2 | `docs/references/design-checklist.md:96` requires forms/buttons with ≥44 px tap targets | Three leave-type buttons forced into 3 columns; labels crowd on smallest phones | Inline `repeat(3, 1fr)` without mobile-first wrapping | `app/dashboard/leave/page.tsx` |

## API / route inventory

Static inventory found 33 pages and 76 API route files. Internal app links resolve to existing pages/APIs; only static asset links (`/favicon.ico`, icons, manifest) are not page routes and are expected assets. Route-specific RBAC behavior is covered by existing tests and by BUG-001 regression update.

## Triage order

1. BUG-001 — PRD §8 role-aware dashboard/navigation acceptance.
2. BUG-002 — destructive action violates reference forbidden contract on core work-location screen.
3. BUG-003 — work-location modal responsive crash risk on 320 px.
4. BUG-004 — reports selector responsive crash risk on 320 px.
5. BUG-005 — raw alert on reports page.
6. BUG-006 — raw confirm on notification page.
7. BUG-007 — leave selector responsive crash risk on 320 px.

## Manual viewport re-check notes

Pending after fixes: re-check affected pages at 320 / 375 / 768 / 1024 / 1440 px. No staging URL available locally.
