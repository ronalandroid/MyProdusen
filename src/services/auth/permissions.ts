export {
  PERMISSIONS,
  canAccessOwnData,
  canAccessTeamData,
  canManageRole,
  hasPermission,
  requirePermission,
} from '@/lib/permissions';

export type { Permission, UserRole } from '@/lib/permissions';
